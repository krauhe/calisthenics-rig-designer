name: Tests
on: [push, pull_request]
permissions:
  contents: read
jobs:
  regression:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: npm ci --ignore-scripts
      - run: python build.py
      - run: git diff --exit-code -- index.html calisthenics-lokal.html vendor/three-source.js
      - run: npm test
      - run: npx playwright install --with-deps chromium
      - run: npm run test:browser
