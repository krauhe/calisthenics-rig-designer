# Local agent / editor config
.claude/

# OS / Dropbox cruft
.DS_Store
Thumbs.db
desktop.ini
*~
*.tmp

# Scratch / temp preview helpers
serve_shot.py

# Node / build output (keep these OUT of Dropbox sync too — see PLAN.md §8)
node_modules/
dist/
